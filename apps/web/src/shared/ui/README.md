# shared/ui

`shared/ui` contains generic reusable UI primitives and generic neutral surfaces.

Current state: this folder currently contains mostly base primitives.

## Import Boundary

`shared/ui` must not import from `entities/*`, `features/*`, or `widgets/*`.

## See Also

- [`../ui-architecture.md`](../ui-architecture.md)
