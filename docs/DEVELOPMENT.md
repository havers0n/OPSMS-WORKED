# Development

This file is intentionally short: development commands are in root `README.md`.

Canonical commands:

```bash
npm install
npm run dev
npm run lint
npm run typecheck
npm run test
```

For local API-only or UI-only debugging, use workspace variants with `--workspace`.
