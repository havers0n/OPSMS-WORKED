# Invariant Matrix

## Purpose

This document is intentionally ruthless.

It exists to separate:

- invariants that are actually enforced
- invariants that are only partially guarded
- invariants that are currently declarative architecture, not runtime truth

If an invariant is not backed by a real enforcement point and real tests, it should not be described as protected.

Snapshot date: `2026-03-13`

## How to read this

- `Owner` = the layer that must keep the invariant true
- `Enforced In` = real mechanisms only, not intent
- `Test Coverage` = current coverage in the repo, not aspirational coverage
- `Notes` = severity plus honesty about status: `implemented`, `partial`, `declared`, or `planned`

## Matrix

| Invariant | Owner | Enforced In | Test Coverage | Notes |
|-----------|-------|-------------|---------------|-------|
| Published layout is the authoritative spatial truth | DB | `layout_versions` state model, publish RPC, published-layout BFF read path | SQL + BFF + E2E | critical; implemented |
| Layout edits happen only in draft; published versions are not edited in place | DB | `create_layout_draft`, `save_layout_draft`, `publish_layout_version` RPC state checks | SQL + E2E | critical; implemented |
| At most one active draft exists per floor | DB | draft lifecycle RPC + DB uniqueness policy | SQL | important; implemented |
| Cells are generated by the system, not authored manually in normal flow | domain + DB | `generatePreviewCells`, `validate_layout_version`, `publish_layout_version` | domain unit + SQL + E2E | critical; partial: strongly enforced on app publish flow, but not sealed against privileged direct SQL writes to `cells` |
| Address semantics are canonical and duplicate generated addresses block publish | domain + DB | address builder/generator, duplicate detection, DB uniqueness on `(layout_version_id, address)` | domain unit + SQL | critical; implemented |
| Rotation is limited to `0/90/180/270` | domain + DB + UI | Zod rack schema, editor `rotateRack()`, DB check constraint | domain schema + UI/store path + SQL | important; implemented |
| Canvas owns spatial manipulation only; structural editing belongs to inspector/forms | frontend | component split, store/actions split, UI affordances | manual + E2E happy path only | important; partial: architecture is clear, but this is not sealed by a hard runtime guard |
| Generated Supabase row types stay inside `api/` boundaries | frontend | ESLint `no-restricted-imports` rules + API mapper convention | lint | important; implemented in static checks only |
| Publish/save/validation lifecycle emits audit events | DB | `operation_events` writes inside layout RPC flow | SQL | important; implemented |
| Storage truth is container-first and location-based, never `SKU -> Cell` and never `GeometrySlot = execution truth` | domain + DB | architecture baseline + ADR-007 + core WMS data model doc | none | critical; declared: canonical target model is now documented, but schema/API convergence is still pending |
| Product-location role is an operational mapping, not physical storage truth | domain + DB | architecture baseline only | none | critical; declared: role/storage separation is documented, not yet enforced in runtime modules |
| Raw import data must not write directly into operational truth tables | DB + import pipeline | architecture baseline only | none | critical; declared: import pipeline is not implemented yet |
| `primary_pick` and `reserve` remain distinct semantics; `reserve` is not a default pick target | domain + DB | architecture baseline only | none | important; declared/planned |
| Same-cell multi-SKU picks must remain separate confirmation steps | domain + ops | architecture baseline only | none | critical; planned: picking execution flow is not implemented yet |
| BFF stays thin; transactional business rules live in Postgres RPC/domain logic | BFF + DB + domain | current route shape and RPC usage pattern | BFF tests indirectly | important; partial: respected in current layout flow, but not mechanically enforced repo-wide |

## Current evidence for this snapshot

- `apps/supabase/migrations/0004_racks_faces_sections_levels_cells.sql`
- `apps/supabase/migrations/0007_layout_draft_lifecycle.sql`
- `apps/supabase/migrations/0009_layout_hardening.sql`
- `apps/supabase/migrations/0010_auth_and_rls.sql`
- `apps/supabase/tests/sql/layout_publish.test.sql`
- `packages/domain/src/layout/generate-cells.ts`
- `packages/domain/src/layout/generate-cells.test.ts`
- `packages/domain/src/layout/validate-layout.ts`
- `packages/domain/src/layout/validate-layout.test.ts`
- `packages/domain/src/layout/rack.ts`
- `apps/web/src/entities/layout-version/model/editor-store.ts`
- `apps/web/e2e/layout-lifecycle.spec.ts`
- `eslint.config.mjs`

## Practical rule

When a new architecture invariant is introduced, it should be added here only after answering all four questions:

1. Who owns it?
2. Where is it enforced?
3. Which tests prove it?
4. Is it truly implemented, or only declared?
